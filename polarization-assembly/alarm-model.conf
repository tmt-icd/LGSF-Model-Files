subsystem = LGSF
component = lp
  
  alarms = [
  {
    name = actuatorFailure
    description = "Some actuator has failed"
    severityLevels = [Major, Warning]
    location = "TBD"
    alarmType = Calculated
    probableCause = "Hardware failure"
    operatorResponse = "Check hardware"
    autoAck = false
    latched = false
  }
  {
    name = sensorFailure
    description = "Polarization sensor has failed"
    severityLevels = [Major, Warning]
    location = "TBD"
    alarmType = Calculated
    probableCause = "Hardware failure"
    operatorResponse = "Check hardware"
    autoAck = false
    latched = false
  }
]