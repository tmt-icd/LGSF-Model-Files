subsystem = LGSF
component = acq

subscribe {
  events = [
    {
      subsystem = TCS
      component = pka
      name      = CurrentPosition //includes zenith angle
    }
    {
      subsystem = TCS
      component = pka
      name      = AssemblyState 
    }
  ]
}
