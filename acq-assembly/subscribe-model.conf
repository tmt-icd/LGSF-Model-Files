subsystem = LGSF
component = acq

subscribe {
  events = [
    {
      subsystem = TCS
      component = pk
      name      = currentPosition //includes zenith angle
    }
    {
      subsystem = TCS
      component = pk
      name      = pointingStatus 
    }
  ]
}
