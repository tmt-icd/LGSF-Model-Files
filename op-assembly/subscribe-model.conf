subsystem = LGSF
component = op

subscribe {
  events = [
    {
      subsystem = TCS
      component = pk
      name      = currentPosition //includes zenith angle
    }
    {
      subsystem = TCS
      component = pk
      name      = pointingStatus 
    }
  ]
}
